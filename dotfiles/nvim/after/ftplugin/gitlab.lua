-- Currently needs `cargo` and SSL libraries installed locally so install it via `pacman`
-- local install_package = require("my-config/utils").install_package
--
-- install_package("gitlab-ci-ls")

vim.lsp.enable("gitlab_ci_ls")
